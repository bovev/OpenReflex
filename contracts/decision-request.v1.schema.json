{
  "$defs": {
    "JsonValue": {}
  },
  "additionalProperties": false,
  "description": "What an API or MCP caller sends: a recipe id and text or a JSON object.",
  "properties": {
    "input": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "additionalProperties": {
            "$ref": "#/$defs/JsonValue"
          },
          "type": "object"
        }
      ],
      "title": "Input"
    },
    "recipe_id": {
      "maxLength": 64,
      "minLength": 1,
      "pattern": "^[a-z0-9]+(?:-[a-z0-9]+)*$",
      "title": "Recipe Id",
      "type": "string"
    }
  },
  "required": [
    "recipe_id",
    "input"
  ],
  "title": "DecisionRequest",
  "type": "object"
}
